import SwiftUI

struct SignupNavButton: View {
    var body: some View {
        CommonButton(title: "회원가입")    }
}

struct SignupNavButton_Previews: PreviewProvider {
    static var previews: some View {
        SignupNavButton()
    }
}
